# Copy this file to .env and fill in the values from your Neon database
# (Vercel dashboard → project → Settings → Environment Variables, or
# `vercel env pull .env` if you have the Vercel CLI linked).

DATABASE_URL="postgres://..."
DATABASE_URL_UNPOOLED="postgres://..."
