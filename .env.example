# Copy this file to .env and fill in the values from your Vercel Postgres
# database (Vercel dashboard → your project → Storage → your database →
# .env.local tab, or `vercel env pull .env` if you have the Vercel CLI linked).

POSTGRES_PRISMA_URL="postgres://..."
POSTGRES_URL_NON_POOLING="postgres://..."
